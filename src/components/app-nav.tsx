"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { CalendarIcon, HomeIcon, MessageIcon, SearchIcon, UserIcon, WalletIcon } from "@/components/icons";
const clientLinks=[{href:"/dashboard",label:"Home",Icon:HomeIcon},{href:"/vendors/browse",label:"Vendors",Icon:SearchIcon},{href:"/events",label:"Events",Icon:CalendarIcon},{href:"/payments",label:"Payments",Icon:WalletIcon},{href:"/messages",label:"Messages",Icon:MessageIcon}];
const vendorLinks=[{href:"/vendor/dashboard",label:"Home",Icon:HomeIcon},{href:"/vendor/leads",label:"Leads",Icon:SearchIcon},{href:"/vendor/payments",label:"Payments",Icon:CalendarIcon},{href:"/messages",label:"Messages",Icon:MessageIcon},{href:"/vendor/onboarding",label:"More",Icon:UserIcon}];
function active(p:string,h:string){if(h==="/dashboard"||h==="/vendor/dashboard")return p===h;if(h==="/events")return p==="/events"||p.startsWith("/events/");return p===h||p.startsWith(`${h}/`)}
export function AppNav({isVendor,isAdmin}:{isVendor:boolean;isAdmin:boolean}){const p=usePathname();const links=[...(isVendor?vendorLinks:clientLinks)];if(isAdmin)links.push({href:"/admin",label:"Admin",Icon:UserIcon});return <><nav className="hidden items-center gap-1 sm:flex">{links.map(({href,label})=><Link key={href} href={href} className={`rounded-lg px-3 py-2 text-[13px] font-medium transition ${active(p,href)?"bg-plum-50 text-plum-800":"text-ink-600 hover:text-plum-800"}`}>{label}</Link>)}</nav><nav style={{gridTemplateColumns:`repeat(${Math.min(links.length,5)},minmax(0,1fr))`}} className="fixed inset-x-0 bottom-0 z-50 grid border-t border-[#E8E1ED] bg-white/95 px-2 pb-[max(8px,env(safe-area-inset-bottom))] pt-1 backdrop-blur-xl sm:hidden">{links.slice(0,5).map(({href,label,Icon})=><Link key={href} href={href} className={`flex min-w-0 flex-col items-center gap-0.5 px-1 py-1.5 text-[9px] font-medium ${active(p,href)?"text-plum-700":"text-ink-500"}`}><Icon size={18}/><span>{label}</span></Link>)}</nav></>}
