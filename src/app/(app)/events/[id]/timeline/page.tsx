import { notFound } from "next/navigation"; import { requireProfile } from "@/lib/auth"; import { createClient } from "@/lib/supabase/server"; import { Card } from "@/components/ui"; import { EventWorkspaceHeader } from "@/components/event/event-workspace-header"; import { SparkleIcon } from "@/components/icons";
export default async function Page({params}:{params:{id:string}}){await requireProfile();const s=createClient();const {data:event}=await s.from("events").select("*").eq("id",params.id).single();if(!event)notFound();return <div className="space-y-7"><EventWorkspaceHeader event={event} active="/timeline" eyebrow="Event workspace"/><Card variant="feature" className="py-14 text-center"><span className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-plum-50 text-plum-700"><SparkleIcon size={24}/></span><p className="mt-5 fleora-kicker">Coming in Fleora V3</p><h1 className="mt-2 font-display text-4xl text-ink-900">Timeline</h1><p className="mx-auto mt-3 max-w-xl text-sm leading-relaxed text-ink-600">Turn your plan into deadlines, vendor arrival times and eventually an Event Day Mode.</p></Card></div>}
