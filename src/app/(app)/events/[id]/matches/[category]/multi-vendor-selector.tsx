"use client";
import{useState}from"react";
import{Button}from"@/components/ui";
export function MultiVendorSelector({vendorIds,action,serviceLabel}:{vendorIds:string[];action:(formData:FormData)=>void;serviceLabel:string}){
 const[selected,setSelected]=useState<string[]>([]);const toggle=(id:string)=>setSelected(s=>s.includes(id)?s.filter(x=>x!==id):s.length<5?[...s,id]:s);
 const suggested=`Hi! I’d love to get a quote for ${serviceLabel} for my event. Please let me know about your availability and pricing. Thank you!`;
 return <form action={action} className="mb-6 rounded-2xl border border-plum-100 bg-plum-50/60 p-4 sm:p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-sm font-bold text-ink-900">Contact a few matches at once</p><p className="mt-1 text-xs leading-relaxed text-ink-600">Choose up to 5 vendors, then personalize the message they’ll receive.</p></div><span className="rounded-full bg-white px-3 py-1 text-xs font-bold text-plum-700">{selected.length}/5 selected</span></div><div className="mt-4 flex flex-wrap gap-2">{vendorIds.map((id,i)=><button key={id} type="button" onClick={()=>toggle(id)} className={`rounded-full border px-3 py-2 text-xs font-semibold transition ${selected.includes(id)?"border-plum-600 bg-plum-600 text-white":"border-plum-200 bg-white text-ink-700"}`}>Vendor {i+1}{selected.includes(id)?" ✓":""}</button>)}</div>{selected.map(id=><input key={id} type="hidden" name="vendor_ids" value={id}/>)}<label className="mt-4 block text-xs font-bold uppercase tracking-wide text-ink-500">Your message</label><p className="mt-1 text-xs text-ink-500">We started it for you — edit it however you’d like.</p><textarea name="message" rows={5} defaultValue={suggested} className="mt-2 w-full rounded-xl border border-plum-100 bg-white px-3 py-2.5 text-sm"/><div className="mt-3 flex justify-end"><Button type="submit" disabled={selected.length===0}>Send {selected.length||""} {selected.length===1?"inquiry":"inquiries"}</Button></div></form>
}
