import Link from "next/link";
import { requireProfile } from "@/lib/auth";
import { initials } from "@/lib/format";
import { AppNav } from "@/components/app-nav";
import { BrandLogo } from "@/components/brand-logo";
export default async function AppLayout({children}:{children:React.ReactNode}){const profile=await requireProfile();const isVendor=profile.account_type==="vendor";const isAdmin=profile.account_type==="admin";return <div className="min-h-screen bg-[#FCFBFD] pb-24 sm:pb-0"><header className="sticky top-0 z-40 border-b border-[#EDE7F1] bg-white/92 backdrop-blur-xl"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-3 sm:px-8"><div className="flex items-center gap-8"><BrandLogo href={isVendor?"/vendor/dashboard":"/dashboard"} compact/><AppNav isVendor={isVendor} isAdmin={isAdmin}/></div><div className="flex items-center gap-3"><span className="hidden text-xs font-medium text-ink-600 md:inline">{profile.first_name} {profile.last_name}</span><Link href={isVendor?"/vendor/onboarding":"/profile"} className="grid h-8 w-8 place-items-center rounded-full border border-plum-100 bg-plum-50 text-[10px] font-semibold text-plum-800">{initials(profile.first_name,profile.last_name)}</Link><form action="/auth/signout" method="post" className="hidden lg:block"><button className="text-[11px] font-medium text-ink-400 hover:text-plum-800">Sign out</button></form></div></div></header><main className="mx-auto max-w-7xl px-5 py-6 sm:px-8 sm:py-8">{children}</main></div>}
