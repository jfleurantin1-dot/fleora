"use server";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";export async function submitRsvp(token:string,fd:FormData){const s=createClient();const r=String(fd.get("rsvp")) as "yes"|"no";await s.rpc("submit_public_rsvp",{p_token:token,p_rsvp:r,p_party_size:Number(fd.get("party_size"))||1,p_dietary:String(fd.get("dietary")??"")||null,p_plus_one_name:String(fd.get("plus_one_name")??"")||null});redirect(`/rsvp/${token}?saved=1`)}
